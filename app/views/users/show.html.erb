<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<p>
<p>
  <strong>First Micropost:</strong>
  <%if @users_first_micropost.nil? %>
    No Microposts Yet
  <% else %>
    <em><%= @users_first_micropost.created_at.strftime("%D") %></em>:
    <%= @users_first_micropost.content %>
  <% end %>
</p>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>

<h2>Debug</h2>
@users_first_micropost.nil? returns <%= @users_first_micropost.nil? %>
